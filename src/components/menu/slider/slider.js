import React from 'react';
import './slider.scss';

class Slider extends React.Component {
	render() {
		return (
			/* <div className={`menu__slider ${!this.props.state.slider.active ? 'menu__slider--hidden' : 'menu__slider--active'}`}> */
			<div className={`menu__slider`}>
				Slider
			</div>
		)
	}
}

export default Slider;
